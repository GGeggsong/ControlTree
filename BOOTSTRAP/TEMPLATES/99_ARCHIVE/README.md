# 99 Archive

> 封存區。**不取代** `00`–`04`、端別資料夾、`_SHARED/` 內的新內容。

---

## 用途

- 已淘汰的舊規格／舊指南
- 雲端衝突檔（例：`xxx (conflicted copy).md`）
- 結束的 phase 報告
- 重大調整前的快照

## 規則

- **只新增，不修改**。封存的東西一律不再變動。
- 命名建議：`<YYYYMMDD>_<topic>.md` 或 `<source>/`。
- 在新檔（仍在 `00`–`04`、`_SHARED/` 或端別資料夾）放一個指向本資料夾的連結，避免資訊斷裂。

## 索引

| 日期 | 主題 | 路徑 | 備註 |
|---|---|---|---|
| — | — | — | — |
